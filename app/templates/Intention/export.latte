{var $title = 'Intence'}

{block content}

<h2>Rozpis intencí na týden od <strong>{$date|czechDate}</strong></h2>
<a n:href="Intention:exportFull, date => $date" class="noprint">včetně celebrantů</a>
<div class="box">
{snippet intention}
<table id="intention" class="table table-bordered table-condensed">
	<tr>
		<th>datum</th>
		<th>intence</th>
	</tr>
	{if count($items) > 0}
	{foreach $items as $item}
	<tr n:class="$item[sun] ? info">
		<td>{if isset($item[date])}<strong>{$item[date]|czechDate:"abbr"|firstUpper}</strong> {/if}{$item[time]|date:"G.i"}</td>
		<td>{$item[intention]}</td>
	</tr>
	{/foreach}
	{/if}
</table>
{/snippet}
</div>

{/block}

